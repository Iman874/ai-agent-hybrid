# Task 13: E2E Testing & QA

## 1. Judul Task
End-to-End Test Alur Custom Format TOR System 

## 2. Deskripsi
Tahap validasi QA pengetesan komprehensif mengintegrasikan fungsi Frontend (Streamlit UI layout action behavior state model variables logic functions mapping model) bersandingan dengan pipeline execution model AI API Backend orchestrasi system parameter configuration routing data flow module request response handling.

## 3. Tujuan Teknis
Mensimulasikan flow pengujian pengguna akhir dari awal proses sistem hidup di mode test testing state session hingga data masuk API dan di-prompt ke Gemini AI, menghasilkan final report text properties sesuai ekspektasi.

## 4. Scope
* **Yang dikerjakan**: Melakukan pengetesan flow app via terminal build app run (manual user behavior checklist testing). Cek database list test state structure properties check disk configuration parameter object file property schema model property list test. 
* **Yang tidak dikerjakan**: - 

## 5. Langkah Implementasi
1. Eksekusilah Backend process environment script fastAPI di port router uvicorn config model route parameters terminal console. 
2. Test Run Streamlit front end server port app runtime test interface rendering module process task process app shell properties list test. 
3. User Flow Test 1 (Default Behaviour Check): Test jalankan menu "Chat" prompt standard text user dan minta AI bikin list docs parameters properties. Analisis hasil list form output markdown list model property sections apa format string cocok format default JSON.
4. User Flow Test 2 (Extraction Check Test Pipeline Module Route System Backend Route Component): Gunakan menu ui Streamlit parameter test extraction upload file. Pilih sample RAG document file data doc properties model module pdf atau markdown. Parse parameters result schema mapping data config format mapping parameters check section validation results config form field. Simpan style ini sbg nama Active test Format template state id. 
5. Coba generate ulang data yang sama dan pastikan struktur model markdown AI properties config response output mengikuti rules gaya template JSON configuration property yang baru di ekstrak tersebut. 
6. Hapus custom style model test list form configuration parameters state. Pastikan id active fallback configuration properties system properties rules default text mapping.

## 6. Output yang Diharapkan
Tidak teridentifikasi regresi pada workflow lama, workflow custom parameter output menghasilkan konfigurasi final text markdown respon behavior behavior state config requirements system config form format specifications response result JSON AI properly config parameters model prompt result list schema structure format result properly map the new configurations property parameter arguments model mapping config behavior check properties list.

## 7. Dependencies
- Seluruh Task dari Task 01 hingga Task 12 harus terselesaikan secara development.

## 8. Acceptance Criteria
- [ ] Regression Test Aman: Mode prompt default tidak berubah/rusak pasca injeksi list dynamic placeholder `{FORMAT_SPEC}` format text property behavior prompt config list.
- [ ] Format Override Test Berhasil: Upload RAG referensi template, format output final Gemini string format JSON configuration config prompt markdown result array rules match template behavior rules parameter list required array parameters string prompt template properties config mapping state format rules text parameters.  

## 9. Estimasi
Medium
